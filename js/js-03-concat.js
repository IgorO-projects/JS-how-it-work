// добавляем работников в штат через "concat"

const oldStaff = ['Mango', 'Poly', 'Ajax'];
const newStaff = ['Chelsy', 'Kiwi'];

const staff = oldStaff.concat(newStaff);

console.log('after :', oldStaff);
console.log('after :', newStaff);
console.log('newArray :', staff);